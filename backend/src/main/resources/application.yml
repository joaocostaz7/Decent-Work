spring:
  application:
    name: decent-work

  datasource:
    url: jdbc:postgresql://localhost:5432/decent_work
    username: jean
    password: zz13818411003
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 110MB

  graphql:
    graphiql:
      enabled: true
      path: /graphiql
    path: /graphql
    schema:
      printer:
        enabled: true
    cors:
      allowed-origins: "*"
      allowed-methods: "*"

server:
  port: 8080

# Web3 Configuration
web3:
  provider-url: https://sepolia.infura.io/v3/YOUR_INFURA_KEY
  escrow-contract-address: 0x0000000000000000000000000000000000000000

app:
  uploads:
    job-attachments-dir: uploads/job-attachments
    bid-attachments-dir: uploads/bid-attachments

# JWT Configuration
jwt:
  secret: GeiJaRDJR6P+ml06XHL2HUM1XOYlz4jHrQvwhgV+xMw=
  expiration: 86400000 # 24 hours

logging:
  level:
    root: info
    org.springframework.security: TRACE
    org.springframework.security.web.FilterChainProxy: TRACE
    com.web3.freelance.security: TRACE
